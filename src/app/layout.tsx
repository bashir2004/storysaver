import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "StorySaver — Download Instagram Stories & Highlights",
  description:
    "Download Instagram stories and highlights for free. No app required. Works on iPhone, Android, and desktop. Only public accounts.",
  keywords: ["instagram story downloader", "instagram highlight downloader", "save instagram stories"],
  openGraph: {
    title: "StorySaver — Download Instagram Stories & Highlights",
    description: "Save Instagram stories and highlights instantly. No app required.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
